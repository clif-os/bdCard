.legendEntry{
  .fa-book{
    position: relative;
    left:5px;
    padding-left: 5px;
  }
  .value{
    font-family: 'Roboto', sans-serif;
    font-weight: 400;
    padding-left: 10px;
  }
}