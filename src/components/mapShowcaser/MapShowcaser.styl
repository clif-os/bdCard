.mapShowcaser {
  overflow: hidden;
}

.mapSelector-container {
  position: absolute;
  top: 0px;
  left: 0px;
  height: 100%;
  width: 100%;
  overflow: hidden;
  z-index: 100;
  pointer-events: none;
}
