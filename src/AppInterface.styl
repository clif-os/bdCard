.AppInterface{
  position: absolute;
  z-index: 100;
  top: 10px;
  left:10px;
  height: 10px;
  .title{
    background-color: rgba(255,255,255,.8)
    border-radius: 4px;
    font-family: 'Montserrat', sans-serif;
    font-weight: 900;
    padding: 3px;
  }
}